#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run the full quality check suite to ensure everything is in order.
# This includes formatting, linting, type checking, and test coverage.
npm run quality

# Stage any automatically fixed files before committing
git add .
